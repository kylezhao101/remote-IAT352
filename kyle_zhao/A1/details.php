<!DOCTYPE html>
<html land='en'>
    <head>
</head>
<body>
    <?php
        if (isset($_GET['id'])) {
            $recipeId = $_GET['id'];

            $csvFile = fopen();
            // TODOS
            // find line with id
            // when found display information

            // decode tags, description, ingredients, and instructions
        }

    ?>
</body>
</html